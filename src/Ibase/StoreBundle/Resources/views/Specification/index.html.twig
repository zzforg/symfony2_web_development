{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Specification list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Fieldname</th>
                <th>Description</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('specification_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.fieldName }}</td>
                <td>{{ entity.description }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('specification_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('specification_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('specification_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
