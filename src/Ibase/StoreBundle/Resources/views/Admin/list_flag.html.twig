{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field%}
    <div>
        {% if not object.getCustomer.getFirstTime %}
            <img alt="old customer" src="{{asset('images/flag.png')}}" 
                 width="20" height="20"/>
        {% endif %}
    </div>
{% endblock %}